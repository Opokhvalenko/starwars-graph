# ── OS / Editors ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# ── Node / Package managers ──────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/

# Keep lockfiles committed:
# !package-lock.json
# !yarn.lock
# !pnpm-lock.yaml

# ── Build / Bundler outputs ──────────────────────────────────────────────────
dist/
build/
.vite/
.vitest/

# ── TypeScript ───────────────────────────────────────────────────────────────
*.tsbuildinfo

# ── Tests / Coverage / Playwright ────────────────────────────────────────────
coverage/
playwright-report/
test-results/
# Playwright traces (if enabled)
*.zip

# ── Storybook ────────────────────────────────────────────────────────────────
storybook-static/

# ── Env & secrets (keep example committed) ───────────────────────────────────
.env
.env.*
!.env.example

# ── Logs / PIDs ──────────────────────────────────────────────────────────────
logs/
*.log
*.pid
*.pid.lock

# ── Docker / Compose (optional overrides, local files) ───────────────────────
*.local
docker-compose.override.yml

# ── Framework / Hosting metadata (optional) ──────────────────────────────────
.vercel/
.netlify/

# ── Misc caches ──────────────────────────────────────────────────────────────
.cache/
.tmp/

# ts
*.tsbuildinfo